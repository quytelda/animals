#ifndef __GLOBAL_H
#define __GLOBAL_H

#define SGN(x) ((x >= 0) ? 1 : -1)
#define MAX(x, y) ((x > y) ? x : y)
#define MIN(x, y) ((x > y) ? y : x)

#define KNRM	"\x1B[0m"
#define KBLU	"\x1B[34m"
#define KGRN	"\x1B[33m"

#endif
